# Chart.yaml now includes dependencies directly
# This file is kept for reference of how dependencies would be structured

dependencies:
  - name: postgresql
    version: 12.x.x
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
    tags:
      - database